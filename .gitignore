# Локальный ~/.gitignore пользователя игнорирует всё (core.excludesfile = *).
# Явно отменяем это для файлов проекта, чтобы git их видел.
!*
!*/

# Локальные настройки Claude Code для этой сессии — не часть проекта.
.claude/
